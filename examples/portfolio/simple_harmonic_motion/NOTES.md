# Portfolio fixture notes

This prompt is used for deterministic orchestration tests. It is not, by itself, evidence that the full generation/render path has been verified.

Future retained outputs should identify whether they came from fixture mode or the real renderer path and should include the configuration, structured plan, generated source, execution log, and measured output metadata needed for reproduction.
