# Quality evidence

A quality metric is portfolio evidence only when it is computed from an actual produced artifact and the implementation of the metric is inspectable.

Hard-coded thresholds, placeholder scores, expected values, or fixture outputs may test plumbing but must not be reported as measured render quality.

For the first verified reference run, one simple inspectable metric is preferable to a large dashboard of synthetic scores.
