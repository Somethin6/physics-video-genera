# End-to-end definition

For this repository, `end-to-end` means the same submitted job travels through real planning/generation configuration, generated-source persistence, sandboxed renderer execution, artifact validation, and measured QA without manually replacing an intermediate result.

Fixture-mode orchestration is integration evidence but is not end-to-end artifact evidence.
